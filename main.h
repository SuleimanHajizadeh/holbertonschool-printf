#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <unistd.h>

/* Prototiplər */
int _printf(const char *format, ...);
int print_char(char c);
int print_string(char *s);

#endif /* MAIN_H */

